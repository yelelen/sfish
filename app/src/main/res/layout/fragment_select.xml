<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:app="http://schemas.android.com/apk/res-auto"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             android:background="@color/bg_color">

    <TextView
        android:id="@+id/label_select_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="select"/>

    <com.yelelen.sfish.view.CircleSeekBar
        android:id="@+id/circleseekbar"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_gravity="center"
        app:cs_progressBarWidth="@dimen/len_2"
        app:cs_secondProgressBarWidth="@dimen/len_1"/>

    <com.yelelen.sfish.view.CircleButton
        android:id="@+id/btn_home"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/len_32"
        app:bgColor="@android:color/black"
        app:imagePadding="@dimen/len_6"
        app:imageSrc="@mipmap/select_small"
        app:ringColor="@android:color/white"/>

</FrameLayout>